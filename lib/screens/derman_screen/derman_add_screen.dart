import 'package:flutter/material.dart';

class DermanAddScreen extends StatelessWidget {
  const DermanAddScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Derman Add Screen"),
      ),
    );
  }
}
