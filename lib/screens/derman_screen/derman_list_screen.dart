import 'package:flutter/material.dart';

class DermanListScreen extends StatelessWidget {
  const DermanListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Derman List Screen"),
      ),
    );
  }
}
