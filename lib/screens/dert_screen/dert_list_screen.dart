import 'package:flutter/material.dart';

class DertListScreen extends StatelessWidget {
  const DertListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Dert List Screen"),
      ),
    );
  }
}
