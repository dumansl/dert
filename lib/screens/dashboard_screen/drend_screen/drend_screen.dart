import 'package:flutter/material.dart';

class DrendScreen extends StatelessWidget {
  const DrendScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Drend Screen"),
    );
  }
}
